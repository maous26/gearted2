// Version Native - vraies constantes de @stripe/stripe-react-native
import { PaymentSheet } from '@stripe/stripe-react-native';

export const PaymentSheetConstants = {
  CollectionMode: PaymentSheet.CollectionMode,
  AddressCollectionMode: PaymentSheet.AddressCollectionMode,
};
